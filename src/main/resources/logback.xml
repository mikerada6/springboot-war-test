<appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <!-- Use the absolute path from application.properties -->
    <file>${LOG_FILE_NAME:-/opt/tomcat/logs/springboot-war-test/springboot-war-test.log}</file>

    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
        <!-- Rolled-over log files will be placed in the same directory -->
        <fileNamePattern>${LOG_FILE_NAME:-/opt/tomcat/logs/springboot-war-test/springboot-war-test}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>

        <maxHistory>30</maxHistory>
        <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
            <maxFileSize>10MB</maxFileSize>
        </timeBasedFileNamingAndTriggeringPolicy>
    </rollingPolicy>

    <encoder>
        <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
    </encoder>
</appender>
